FROM node:lts
ARG MONGODB_URL
ARG PORT
ENV PORT=${PORT}
ENV MONGODB_URL=${MONGODB_URL}

RUN mkdir -p /app/service1/
WORKDIR /
COPY /app/service1/package.json /
RUN npm install
COPY . .

EXPOSE 3000

CMD [ "npm", "start" ]
