FROM node:lts
ARG MONGODB_URL
ARG PORT
ENV PORT=${PORT}
ENV MONGODB_URL=${MONGODB_URL}

RUN mkdir -p /app/service1/
WORKDIR /app/service1/
COPY package.json /app/service1/
RUN npm install
COPY . /app/service1/

COPY . .

EXPOSE 3000

CMD [ "npm", "start" ]
